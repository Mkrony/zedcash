import Lottie from "lottie-react";
import Spinner from '../../assets/animation/SpinnerLottie.json';
function CashoutAnimation({size}) {
    return (
        <div>
            <Lottie animationData={Spinner} width={size} loop={true}/>
        </div>
    )
}

export default CashoutAnimation
